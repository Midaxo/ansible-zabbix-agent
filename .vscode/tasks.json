{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "cd tests/ && vagrant up && vagrant destroy --force",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}